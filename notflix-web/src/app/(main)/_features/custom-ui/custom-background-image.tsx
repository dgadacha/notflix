/**
 * Custom background image — Notflix is pure-black and ships no user
 * background. Stub kept so __root.tsx imports stay valid.
 */
import React from "react"

export function CustomBackgroundImage() {
    return null
}
